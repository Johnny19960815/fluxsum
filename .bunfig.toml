# Bun 1.3 全局配置

[install]
# 使用 npm registry
registry = "https://registry.npmmirror.com"

# 自动安装 peer dependencies
auto-install-peers = true

# 锁文件格式
lockfile = "bun"

[run]
# 启用 bun shell
shell = true
