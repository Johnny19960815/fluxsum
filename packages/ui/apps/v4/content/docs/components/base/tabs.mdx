---
title: Tabs
description: 一组分层的内容区域——称为选项卡面板——每次只显示一个。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/tabs
  api: https://base-ui.com/react/components/tabs#api-reference
---

<ComponentPreview
  styleName="new-york-v4"
  name="tabs-demo"
  previewClassName="min-h-[32rem]"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令</TabsTrigger>
  <TabsTrigger value="manual">手动安装</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add tabs
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>安装以下依赖：</Step>

```bash
npm install @base-ui/react
```

<Step>将以下代码复制到你的项目中。</Step>

<ComponentSource name="tabs" title="components/ui/tabs.tsx" styleName="new-york-v4" />

<Step>更新导入路径以匹配你的项目结构。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx showLineNumbers
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
```

```tsx showLineNumbers
<Tabs defaultValue="account" className="w-[400px]">
  <TabsList>
    <TabsTrigger value="account">Account</TabsTrigger>
    <TabsTrigger value="password">Password</TabsTrigger>
  </TabsList>
  <TabsContent value="account">Make changes to your account here.</TabsContent>
  <TabsContent value="password">Change your password here.</TabsContent>
</Tabs>
```

## 组合结构

使用以下组合结构来构建 `Tabs`：

```text
Tabs
├── TabsList
│   ├── TabsTrigger
│   └── TabsTrigger
├── TabsContent
└── TabsContent
```

## 样式变体

`TabsList` 支持 `variant="nav"`：悬停时显示 `muted` 背景，选中项仅显示底部主题色（`primary`）指示条，无整块选中背景。

```tsx showLineNumbers
<TabsList variant="nav">
  <TabsTrigger value="home">Home</TabsTrigger>
  <TabsTrigger value="components">Components</TabsTrigger>
</TabsList>
```

## 示例


{/* ComponentPreview name="tabs-line" removed - example not in registry */}


{/* ComponentPreview name="tabs-vertical" removed - example not in registry */}


{/* ComponentPreview name="tabs-disabled" removed - example not in registry */}


{/* ComponentPreview name="tabs-icons" removed - example not in registry */}

## RTL 支持

如需启用 RTL 支持，请参阅 [RTL 配置指南](/docs/rtl)。

{/* <ComponentPreview styleName="new-york-v4" name="tabs-rtl" direction="rtl" /> */}

## API 参考

更多信息请参阅 [Base UI Tabs](https://base-ui.com/react/components/tabs#api-reference) 文档。
