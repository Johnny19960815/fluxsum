---
title: Input
description: 用于表单和用户数据输入的文本输入组件，内置样式和无障碍功能。
base: base
component: true
---

<ComponentPreview
  styleName="new-york-v4"
  name="input-demo"
  previewClassName="*:max-w-xs"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令</TabsTrigger>
  <TabsTrigger value="manual">手动安装</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add input
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>将以下代码复制到你的项目中。</Step>

<ComponentSource name="input" title="components/ui/input.tsx" styleName="new-york-v4" />

<Step>更新导入路径以匹配你的项目结构。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx
import { Input } from "@/components/ui/input"
```

```tsx
<Input />
```

## 示例


{/* ComponentPreview name="input-basic" removed - example not in registry */}


{/* ComponentPreview name="input-field" removed - example not in registry */}


{/* ComponentPreview name="input-fieldgroup" removed - example not in registry */}

### 禁用

使用 `disabled` 属性禁用输入框。要设置禁用状态的样式，请在 `Field` 组件上添加 `data-disabled` 属性。

<ComponentPreview
  styleName="new-york-v4"
  name="input-disabled"
  previewClassName="*:max-w-xs"
/>


{/* ComponentPreview name="input-invalid" removed - example not in registry */}

### 文件

使用 `type="file"` 属性创建文件输入。

<ComponentPreview
  styleName="new-york-v4"
  name="input-file"
  previewClassName="*:max-w-xs"
/>


{/* ComponentPreview name="input-inline" removed - example not in registry */}


{/* ComponentPreview name="input-grid" removed - example not in registry */}


{/* ComponentPreview name="input-required" removed - example not in registry */}


{/* ComponentPreview name="input-badge" removed - example not in registry */}


{/* ComponentPreview name="input-input-group" removed - example not in registry */}


{/* ComponentPreview name="input-button-group" removed - example not in registry */}


{/* ComponentPreview name="input-form" removed - example not in registry */}

## RTL 支持

如需启用 RTL 支持，请参阅 [RTL 配置指南](/docs/rtl)。

{/* <ComponentPreview styleName="new-york-v4" name="input-rtl" direction="rtl" previewClassName="*:max-w-xs" /> */}
