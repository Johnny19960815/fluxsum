---
title: Empty
description: 使用 Empty 组件来显示空状态。
base: radix
component: true
---

<ComponentPreview
  styleName="new-york-v4"
  name="empty-demo"
  previewClassName="h-96 p-0"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令</TabsTrigger>
  <TabsTrigger value="manual">手动安装</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add empty
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>将以下代码复制到你的项目中。</Step>

<ComponentSource name="empty" title="components/ui/empty.tsx" styleName="new-york-v4" />

<Step>更新导入路径以匹配你的项目结构。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx
import {
  Empty,
  EmptyContent,
  EmptyDescription,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
} from "@/components/ui/empty"
```

```tsx
<Empty>
  <EmptyHeader>
    <EmptyMedia variant="icon">
      <Icon />
    </EmptyMedia>
    <EmptyTitle>No data</EmptyTitle>
    <EmptyDescription>No data found</EmptyDescription>
  </EmptyHeader>
  <EmptyContent>
    <Button>Add data</Button>
  </EmptyContent>
</Empty>
```

## 组合结构

使用以下组合结构来构建 `Empty` 空状态：

```text
Empty
├── EmptyHeader
│   ├── EmptyMedia
│   ├── EmptyTitle
│   └── EmptyDescription
└── EmptyContent
```

## 示例

### 描边样式

使用 `border` 工具类创建描边样式的空状态。

<ComponentPreview
  styleName="new-york-v4"
  name="empty-outline"
  previewClassName="h-96 p-6 md:p-10"
/>

### 背景

使用 `bg-*` 和 `bg-gradient-*` 工具类为空状态添加背景。

<ComponentPreview
  styleName="new-york-v4"
  name="empty-background"
  previewClassName="h-96 p-0"
/>

### 头像

使用 `EmptyMedia` 组件在空状态中显示头像。

<ComponentPreview
  styleName="new-york-v4"
  name="empty-avatar"
  previewClassName="h-96 p-0"
/>

### 头像组

使用 `EmptyMedia` 组件在空状态中显示头像组。

<ComponentPreview
  styleName="new-york-v4"
  name="empty-avatar-group"
  previewClassName="h-96 p-0"
/>

### InputGroup

你可以将 `InputGroup` 组件添加到 `EmptyContent` 组件中。

<ComponentPreview
  styleName="new-york-v4"
  name="empty-input-group"
  previewClassName="h-96 p-0"
/>

## RTL 支持

如需启用 RTL 支持，请参阅 [RTL 配置指南](/docs/rtl)。

{/* <ComponentPreview styleName="new-york-v4" name="empty-rtl" direction="rtl" /> */}

## API 参考

### Empty

空状态的主组件。包裹 `EmptyHeader` 和 `EmptyContent` 组件。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<Empty>
  <EmptyHeader />
  <EmptyContent />
</Empty>
```

### EmptyHeader

`EmptyHeader` 组件包裹空状态的媒体、标题和描述。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyHeader>
  <EmptyMedia />
  <EmptyTitle />
  <EmptyDescription />
</EmptyHeader>
```

### EmptyMedia

使用 `EmptyMedia` 组件显示空状态的媒体内容，如图标或图片。你也可以用它来显示其他组件，如头像。

| Prop        | Type                  | Default   |
| ----------- | --------------------- | --------- |
| `variant`   | `"default" \| "icon"` | `default` |
| `className` | `string`              |           |

```tsx
<EmptyMedia variant="icon">
  <Icon />
</EmptyMedia>
```

```tsx
<EmptyMedia>
  <Avatar>
    <AvatarImage src="..." />
    <AvatarFallback>CN</AvatarFallback>
  </Avatar>
</EmptyMedia>
```

### EmptyTitle

使用 `EmptyTitle` 组件显示空状态的标题。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyTitle>No data</EmptyTitle>
```

### EmptyDescription

使用 `EmptyDescription` 组件显示空状态的描述。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyDescription>You do not have any notifications.</EmptyDescription>
```

### EmptyContent

使用 `EmptyContent` 组件显示空状态的内容，如按钮、输入框或链接。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyContent>
  <Button>Add Project</Button>
</EmptyContent>
```
