---
title: Item
description: 用于展示包含媒体、标题、描述和操作的通用组件。
base: radix
component: true
---

<ComponentPreview styleName="new-york-v4" name="item-demo" />

`Item` 组件是一个简洁的 flex 容器，几乎可以容纳任何类型的内容。使用它来展示标题、描述和操作。配合 `ItemGroup` 组件可以创建项目列表。

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令</TabsTrigger>
  <TabsTrigger value="manual">手动安装</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add item
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>将以下代码复制到你的项目中。</Step>

<ComponentSource name="item" title="components/ui/item.tsx" styleName="new-york-v4" />

<Step>更新导入路径以匹配你的项目结构。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx showLineNumbers
import {
  Item,
  ItemActions,
  ItemContent,
  ItemDescription,
  ItemMedia,
  ItemTitle,
} from "@/components/ui/item"
```

```tsx showLineNumbers
<Item>
  <ItemMedia variant="icon">
    <Icon />
  </ItemMedia>
  <ItemContent>
    <ItemTitle>Title</ItemTitle>
    <ItemDescription>Description</ItemDescription>
  </ItemContent>
  <ItemActions>
    <Button>Action</Button>
  </ItemActions>
</Item>
```

## 组合结构

使用以下组合结构来构建 `Item`：

```text
ItemGroup
└── Item
    ├── ItemHeader
    ├── ItemMedia
    ├── ItemContent
    │   ├── ItemTitle
    │   └── ItemDescription
    ├── ItemActions
    └── ItemFooter
```

## Item 与 Field 的区别

如果你需要展示表单输入控件（如复选框、输入框、单选按钮或选择器），请使用 `Field`。

如果你只需要展示标题、描述和操作等内容，请使用 `Item`。

## 变体

使用 `variant` 属性更改项目的视觉样式。

<ComponentPreview
  styleName="new-york-v4"
  name="item-variant"
  previewClassName="h-96"
/>

## 尺寸

使用 `size` 属性更改项目的尺寸。可用尺寸有 `default`、`sm` 和 `xs`。

<ComponentPreview
  styleName="new-york-v4"
  name="item-size"
  previewClassName="h-96"
/>

## 示例

### 图标

使用 `ItemMedia` 配合 `variant="icon"` 来显示图标。

<ComponentPreview styleName="new-york-v4" name="item-icon" />

### 头像

可以使用 `ItemMedia` 配合 `variant="avatar"` 来显示头像。

<ComponentPreview styleName="new-york-v4" name="item-avatar" />

### 图片

使用 `ItemMedia` 配合 `variant="image"` 来显示图片。

<ComponentPreview styleName="new-york-v4" name="item-image" />

### 分组

使用 `ItemGroup` 将相关项目分组在一起。

<ComponentPreview
  styleName="new-york-v4"
  name="item-group"
  previewClassName="h-96"
/>

### 头部

使用 `ItemHeader` 在项目内容上方添加头部。

<ComponentPreview
  styleName="new-york-v4"
  name="item-header"
  previewClassName="h-96"
/>

### 链接

使用 `asChild` 属性将项目渲染为链接。悬停和焦点状态将应用于锚元素。

<ComponentPreview styleName="new-york-v4" name="item-link" />

```tsx showLineNumbers
<Item asChild>
  <a href="/dashboard">
    <ItemMedia variant="icon">
      <HomeIcon />
    </ItemMedia>
    <ItemContent>
      <ItemTitle>Dashboard</ItemTitle>
      <ItemDescription>Overview of your account and activity.</ItemDescription>
    </ItemContent>
  </a>
</Item>
```

### 下拉菜单

<ComponentPreview styleName="new-york-v4" name="item-dropdown" />

## RTL 支持

如需启用 RTL 支持，请参阅 [RTL 配置指南](/docs/rtl)。

{/* <ComponentPreview styleName="new-york-v4" name="item-rtl" direction="rtl" /> */}

## API 参考

### Item

用于展示包含媒体、标题、描述和操作内容的主组件。

| Prop      | Type                                | Default     |
| --------- | ----------------------------------- | ----------- |
| `variant` | `"default" \| "outline" \| "muted"` | `"default"` |
| `size`    | `"default" \| "sm" \| "xs"`         | `"default"` |
| `asChild` | `boolean`                           | `false`     |

### ItemGroup

将相关项目分组在一起并提供一致样式的容器。

```tsx
<ItemGroup>
  <Item />
  <Item />
</ItemGroup>
```

### ItemSeparator

分组中项目之间的分隔线。

```tsx
<ItemGroup>
  <Item />
  <ItemSeparator />
  <Item />
</ItemGroup>
```

### ItemMedia

使用 `ItemMedia` 来显示媒体内容，如图标、图片或头像。

| Prop      | Type                             | Default     |
| --------- | -------------------------------- | ----------- |
| `variant` | `"default" \| "icon" \| "image"` | `"default"` |

```tsx
<ItemMedia variant="icon">
  <Icon />
</ItemMedia>
```

```tsx
<ItemMedia variant="image">
  <img src="..." alt="..." />
</ItemMedia>
```

### ItemContent

包裹项目的标题和描述。

```tsx
<ItemContent>
  <ItemTitle>Title</ItemTitle>
  <ItemDescription>Description</ItemDescription>
</ItemContent>
```

### ItemTitle

显示项目的标题。

```tsx
<ItemTitle>Item Title</ItemTitle>
```

### ItemDescription

显示项目的描述。

```tsx
<ItemDescription>Item description</ItemDescription>
```

### ItemActions

操作按钮或其他交互元素的容器。

```tsx
<ItemActions>
  <Button>Action</Button>
</ItemActions>
```

### ItemHeader

在项目内容上方显示头部。

```tsx
<Item>
  <ItemHeader>Header</ItemHeader>
  <ItemContent>...</ItemContent>
</Item>
```

### ItemFooter

在项目内容下方显示底部。

```tsx
<Item>
  <ItemContent>...</ItemContent>
  <ItemFooter>Footer</ItemFooter>
</Item>
```
