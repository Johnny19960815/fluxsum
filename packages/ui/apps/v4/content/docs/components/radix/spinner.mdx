---
title: Spinner
description: 一个可用于显示加载状态的指示器。
base: radix
component: true
---

<ComponentPreview styleName="new-york-v4" name="spinner-demo" />

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令</TabsTrigger>
  <TabsTrigger value="manual">手动安装</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add spinner
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>将以下代码复制到你的项目中。</Step>

<ComponentSource name="spinner" title="components/ui/spinner.tsx" styleName="new-york-v4" />

<Step>更新导入路径以匹配你的项目结构。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx
import { Spinner } from "@/components/ui/spinner"
```

```tsx
<Spinner />
```

## 自定义

你可以通过编辑 `Spinner` 组件将默认的加载图标替换为任何其他图标。

<ComponentPreview styleName="new-york-v4" name="spinner-custom" />

```tsx showLineNumbers title="components/ui/spinner.tsx"
import { LoaderIcon } from "lucide-react"

import { cn } from "@/lib/utils"

function Spinner({ className, ...props }: React.ComponentProps<"svg">) {
  return (
    <LoaderIcon
      role="status"
      aria-label="Loading"
      className={cn("size-4 animate-spin", className)}
      {...props}
    />
  )
}

export { Spinner }
```

## 示例

### 尺寸

使用 `size-*` 工具类来更改 spinner 的尺寸。

<ComponentPreview styleName="new-york-v4" name="spinner-size" />

### 按钮

将 spinner 添加到按钮中以指示加载状态。将 `<Spinner />` 放在标签前并设置 `data-icon="inline-start"` 表示起始位置，或放在标签后并设置 `data-icon="inline-end"` 表示结束位置。

<ComponentPreview styleName="new-york-v4" name="spinner-button" />

### 徽章

将 spinner 添加到徽章中以指示加载状态。将 `<Spinner />` 放在标签前并设置 `data-icon="inline-start"` 表示起始位置，或放在标签后并设置 `data-icon="inline-end"` 表示结束位置。

<ComponentPreview styleName="new-york-v4" name="spinner-badge" />

### 输入框组

<ComponentPreview styleName="new-york-v4" name="spinner-input-group" />

### 空状态

<ComponentPreview styleName="new-york-v4" name="spinner-empty" />

## RTL 支持

如需启用 RTL 支持，请参阅 [RTL 配置指南](/docs/rtl)。

{/* <ComponentPreview styleName="new-york-v4" name="spinner-rtl" direction="rtl" /> */}
