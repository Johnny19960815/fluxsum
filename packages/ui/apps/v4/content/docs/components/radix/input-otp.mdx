---
title: Input OTP
description: 支持复制粘贴功能的无障碍一次性密码组件。
base: radix
component: true
links:
  doc: https://input-otp.rodz.dev
---

<ComponentPreview styleName="new-york-v4" name="input-otp-demo" />

## 关于

Input OTP 基于 [@guilherme_rodz](https://twitter.com/guilherme_rodz) 开发的 [input-otp](https://github.com/guilhermerodz/input-otp) 构建。

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令</TabsTrigger>
  <TabsTrigger value="manual">手动安装</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add input-otp
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>安装以下依赖：</Step>

```bash
npm install input-otp
```

<Step>将以下代码复制到你的项目中。</Step>

<ComponentSource name="input-otp" title="components/ui/input-otp.tsx" styleName="new-york-v4" />

<Step>更新导入路径以匹配你的项目结构。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx showLineNumbers
import {
  InputOTP,
  InputOTPGroup,
  InputOTPSeparator,
  InputOTPSlot,
} from "@/components/ui/input-otp"
```

```tsx showLineNumbers
<InputOTP maxLength={6}>
  <InputOTPGroup>
    <InputOTPSlot index={0} />
    <InputOTPSlot index={1} />
    <InputOTPSlot index={2} />
  </InputOTPGroup>
  <InputOTPSeparator />
  <InputOTPGroup>
    <InputOTPSlot index={3} />
    <InputOTPSlot index={4} />
    <InputOTPSlot index={5} />
  </InputOTPGroup>
</InputOTP>
```

## 组合结构

使用以下组合结构来构建 `InputOTP`：

```text
InputOTP
├── InputOTPGroup
│   ├── InputOTPSlot
│   ├── InputOTPSlot
│   └── InputOTPSlot
├── InputOTPSeparator
├── InputOTPGroup
│   ├── InputOTPSlot
│   ├── InputOTPSlot
│   └── InputOTPSlot
├── InputOTPSeparator
└── InputOTPGroup
    ├── InputOTPSlot
    └── InputOTPSlot
```

## 模式

使用 `pattern` 属性为 OTP 输入定义自定义模式。

```tsx showLineNumbers {1,5}
import { REGEXP_ONLY_DIGITS_AND_CHARS } from "input-otp"

;<InputOTP maxLength={6} pattern={REGEXP_ONLY_DIGITS_AND_CHARS}>
  ...
</InputOTP>
```

<ComponentPreview styleName="new-york-v4" name="input-otp-pattern" />

## 示例

### 分隔符

使用 `<InputOTPSeparator />` 组件在输入组之间添加分隔符。

<ComponentPreview styleName="new-york-v4" name="input-otp-separator" />


{/* ComponentPreview name="input-otp-disabled" removed - example not in registry */}

### 受控模式

使用 `value` 和 `onChange` 属性来控制输入值。

<ComponentPreview styleName="new-york-v4" name="input-otp-controlled" />


{/* ComponentPreview name="input-otp-invalid" removed - example not in registry */}


{/* ComponentPreview name="input-otp-four-digits" removed - example not in registry */}


{/* ComponentPreview name="input-otp-alphanumeric" removed - example not in registry */}


{/* ComponentPreview name="input-otp-form" removed - example not in registry */}

## RTL 支持

如需启用 RTL 支持，请参阅 [RTL 配置指南](/docs/rtl)。

{/* <ComponentPreview styleName="new-york-v4" name="input-otp-rtl" direction="rtl" /> */}

## API 参考

更多信息请参阅 [input-otp](https://input-otp.rodz.dev) 文档。
